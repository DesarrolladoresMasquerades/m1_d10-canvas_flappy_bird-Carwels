class Obstacles {
    constructor(ctx) {
        this.ctx = ctx
        this.objects = []
    }

    move(frameNumber) {
        console.log("obstacles move to frame number: ", frameNumber)
    }

    draw(frameNumber) {
        console.log("obstacles move to frame number: ", frameNumber)
    }
}